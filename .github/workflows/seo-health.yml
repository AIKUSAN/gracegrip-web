name: Weekly SEO Health

on:
  schedule:
    - cron: '17 13 * * 1'
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  health:
    name: Weekly crawl and canonical check
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: Debug — curl homepage headers
        run: |
          curl -sI -A "GraceGripSEOHealth/1.0" https://gracegrip.app/ | head -20
          echo "---"
          curl -sI -A "GraceGripSEOHealth/1.0" -H "x-vercel-protection-bypass: ${{ secrets.VERCEL_AUTOMATION_BYPASS_SECRET }}" https://gracegrip.app/ | head -20

      - name: Run SEO health check
        env:
          SEO_HEALTH_OUTPUT_DIR: seo-health
          VERCEL_AUTOMATION_BYPASS_SECRET: ${{ secrets.VERCEL_AUTOMATION_BYPASS_SECRET }}
        run: node scripts/seo-health-check.mjs

      - name: Upload SEO health artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: seo-health-report
          path: seo-health

      - name: Publish summary
        if: always()
        run: |
          if [ -f seo-health/seo-health-report.md ]; then
            cat seo-health/seo-health-report.md >> "$GITHUB_STEP_SUMMARY"
          else
            echo "No SEO health report generated." >> "$GITHUB_STEP_SUMMARY"
          fi
